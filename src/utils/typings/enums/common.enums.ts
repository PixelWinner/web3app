export enum LocalStorage {
    USER_NAME = "userName",
}

export enum MessageType {
    USER = "user",
    SYSTEM = "system",
}

export enum EventType {
    JOIN = "join",
    MESSAGE = "message",
    LOAD_MESSAGES ="loadMessages"
}